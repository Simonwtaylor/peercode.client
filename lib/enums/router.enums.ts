export enum RouterEnums {
  HOME = '/',
  SESSIONS = '/sessions',
  CHAT = '/chat',
  CONTRACTS = '/contracts',
  CONTRACT = '/contracts/{slug}',
  EXPLORE = '/explore',
  USER = '/user/{slug}',
  ARTICLES = '/articles',
  SETTINGS = '/settings',
}