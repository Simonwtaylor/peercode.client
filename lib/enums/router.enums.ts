export enum RouterEnums {
  HOME = '/',
  CALL = '/call',
  JOBS = '/jobs',
  CREATE_JOB = '/create-job',
  FEED = '/feed',
  LOGIN = '/login',
  PROFILE = '/profile/{slug}',
  JOB = '/job-posting/{slug}',
  APPLICATION = '/application/{slug}',
  APPLICATIONS = '/applications'
}